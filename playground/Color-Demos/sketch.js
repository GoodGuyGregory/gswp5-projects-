//  Variables:


function setup() {
    createCanvas(windowWidth, windowHeight);
    background(50);
}

function draw() {
    c = color(12, 255, 45);
    fill(c);
    ellipse(windowWidth / 2, windowHeight / 2, 350, 350);
}

