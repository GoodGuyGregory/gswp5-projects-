//  Variables:

function setup() {
    createCanvas(1980, 1020);
}

function draw() {
    background(204);
    line(0, 0, width, height);
    line(width, 0, 0, height);
    ellipse(width / 2, height / 2, 260, 260); //left










}